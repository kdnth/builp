[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Client-side routing (react-router). Let the app handle every path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
