[variables]
NIXPACKS_PYTHON_PACKAGE_MANAGER = "uv"

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install uv==0.6.10",
  ". /opt/venv/bin/activate && uv sync --no-dev --frozen",
]
paths = ["/opt/venv/bin"]
cacheDirectories = ["/root/.cache/uv"]
